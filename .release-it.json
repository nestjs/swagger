{
  "git": {
    "commitMessage": "chore(): release v${version}"
  },
  "github": {
    "release": true,
    "releaseNotes": "npx lerna-changelog --from ${latestTag} --to ${tagName}",
    "web": true
  }
}
